{
  "name": "fake-server",
  "version": "1.0.0",
  "description": "Fake rest api",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watchdb": "json-server --watch db.json"
  },
  "author": "Melly",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
